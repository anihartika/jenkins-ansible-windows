name: Execute Playbook

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: 3.9.0


      - name: Install Ansible
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Execute playbook
        run: |
          ansible-playbook windowsplaybook.yml
